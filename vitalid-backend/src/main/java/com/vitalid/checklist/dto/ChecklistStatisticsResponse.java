package com.vitalid.checklist.dto;

import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * DTO para estadísticas del checklist
 * 
 * TODO: Implement ChecklistStatisticsResponse with:
 * - adherenceRate (percentage)
 * - medicationsTakenOnTime (count)
 * - missedDoses (count)
 * - skippedDoses (count)
 * - trend (e.g., "IMPROVING", "DECLINING", "STABLE")
 */
@Data
@NoArgsConstructor
public class ChecklistStatisticsResponse {

    // TODO: Add properties

}

